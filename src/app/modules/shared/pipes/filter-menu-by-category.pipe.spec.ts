import { FilterMenuByCategoryPipe } from './filter-menu-by-category.pipe';

describe('FilterMenuByCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMenuByCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
